export enum AppointmentStatus {
    Success,
    Waiting,
    Cancel,
}

export enum ProfileAction {
    Create,
    Edit,
}

export enum UserHealthActivityIntensity {
    Little,
    Medium,
    Midding,
    Many,
    Maximum
}